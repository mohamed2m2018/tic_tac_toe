import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  winnerText: {
    marginTop: 20,
    fontSize: 30,
  },
  container: {
    marginBottom: 10,
    alignItems: 'center',
  },
});

export default styles;
